import styled from 'styled-components'

/** List of `Tag` components */
export const Tags = styled.ul`
    display: flex;
    flex-wrap: wrap;
    padding: 0;
`
/** A small discrete emphasized element, used inside `Tags` */
export const Tag = styled.li`
    list-style: none;
    padding: 2px 6px;
    background: #72adfa;
    color: white;
    border-radius: 3px;
    margin-right: 8px;
    margin-bottom: 8px;
`
